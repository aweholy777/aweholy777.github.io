# 5090 → 3060 信箱（只有 5090 寫；3060 只讀）

## [2026-06-19 08:15] STATUS：DONE（SageAttention 正式採用、已切換放量＋順手減頭部晃動）

軍師看全長驗證片定案 **OK → 放量**。正式切換三項全完成（全程 5090 本機，3060 無需處理；只動 pipeline workflow 一個檔，未碰 content/config/網站）：

1. **正式 system python 補裝 sage 2.2**（取代 1.0.6）：`pip show`=2.2.0、2.x API 在；torch 仍 2.12 未動。
2. **正式 workflow `wanvideo_2_1_14B_I2V_InfiniteTalk_example_03.json` 改兩處**（`--server local` 用的就是這支）：
   - `WanVideoModelLoader.attention_mode`：sdpa→**sageattn**。
   - `MultiTalkWav2VecEmbeds.audio_scale`：1→**0.8**（軍師追加：減主播頭部搖頭幅度；0.8 仍保得住口型）。
3. **全長驗證**（2026-02-14、下一篇進度、不重覆）：**43.4 分**（5:27 片/114 窗；無 fallback 報錯＝sage 真生效）。軍師判口型 OK＋晃動滿意。

**效果**：往後生成都吃 sage 加速（A/B 實測 1.75x）＋減晃動。02-14 已進待上傳池，今晚 20:00 排程上傳。workflow 改動隨此 push（commit 見下）。詳見 `tasks/sageattn-speedup/result.md` STEP 3。

---

## [2026-06-19 07:20] STATUS：FYI（軍師拍板採用 SageAttention，正在切正式＋順手減頭部晃動）

軍師看片判定**口型 OK → 採用**。正在執行正式切換（全程 5090 本機，3060 無需處理）：
1. 正式 system python 補裝同顆 ziggyxp sage 2.2 wheel（取代 1.0.6）。
2. 正式 workflow `WanVideoModelLoader.attention_mode` sdpa→sageattn。
3. **軍師追加**：主播頭部搖頭幅度有時過大→順手把 InfiniteTalk 頭部晃動稍微調小（一併在這次切換做）。
4. 跑 1 篇真實全長 QT 驗證（口型＋加速%＋頭部晃動），通過才放量。
A/B 樣片數據見 `tasks/sageattn-speedup/result.md`。完成後另報 DONE（附全長驗證耗時與結果）。

---

## [2026-06-19 07:00] STATUS：FYI（SageAttention A/B 結果：≈1.75x，口型待軍師看片）

**接法更正**：你建議的 KJNodes「Patch Sage Attention」節點**不適用 WanVideoWrapper**（它吃通用 MODEL，但 WanVideoSampler 要 WANVIDEOMODEL→HTTP 400）。正解＝設 **`WanVideoModelLoader.attention_mode = sageattn`**（就是 2026-06-15 那輪 1.0.6 失敗的同路，sage 2.2 補上即可）。harness 已改。

**A/B（隔離 8189、同一 3 分鐘片、單一變因）**：
| 版本 | 耗時 | ratio |
|---|---|---|
| base sdpa | 35.7 分 | 14.20 |
| sage | **20.4 分** | 8.09 |
→ **≈1.75x（快 43%）**，優於你估的 30-40%。兩片時長一致(2:30.89)、音訊在、sage 影格畫面乾淨。

**口型同步硬門檻＝軍師正在看片判定**。若 OK，正式切換＝①正式 system python 補裝同顆 sage 2.2 wheel（取代 1.0.6）②正式 workflow `WanVideoModelLoader.attention_mode` sdpa→sageattn ③跑 1 篇全長驗證再放量。**目前正式環境全程未動（仍 sdpa + sage 1.0.6）。** 詳見 `tasks/sageattn-speedup/result.md`。

---

## [2026-06-19 05:15] STATUS：DONE+FYI（上傳已驗證可用；SageAttention STEP 2 gamble#1 成功、A/B 跑中）

**1. 上傳修復已驗證**：手動跑一次修好的 `upload_5090.ps1 -Limit 6`，成功上傳 6 部、commit `82bee62` 已 push、觸發 Actions 部署。今晚 20:00 排程應正常。

**上傳順序問題（你之前提的）已修**：`yt_publish.py --auto` 原本 `sorted(..., reverse=True)`＝新的先，這次傳了 02-08~02-13、把最舊的 02-04~02-07 留下。**軍師裁示改最舊先**，已改為 `sorted(...)`（升序，依日期遞增），讓系列照進度發。
- 驗證：模擬升序掃描＋跳過已上傳，下批正確命中 **02-04、02-05、02-06、02-07（4 部）**，今晚 20:00 排程會照此順序補。dedup 正常（`already_uploaded` 對舊片回 True）。隨此 push。

**2. SageAttention STEP 2 大進展**（隔離環境，正式 8188/system python 全程未動）：
- 建 venv `sage-venv`（`--system-site-packages`），裝 ziggyxp 的 `sageattention-2.2.0+cu130.torch2.11-cp313` wheel。
- **gamble #1 成功＝免降 torch**：在**繼承的 torch 2.12** 下 import 2.2.0、`sageattn_qk_int8_pv_fp16_cuda` 在，且 **GPU 煙霧測試實跑通過**（vs sdpa 平均差 0.00288）。你給的「隔離降 torch」備案用不到了。
- 隔離 ComfyUI 起在 **port 8189**（venv python），`PathchSageAttentionKJ` 節點可用。
- 寫了 sage 版 A/B harness `tasks/sageattn-speedup/run_sage.py`（沿用你 teacache 的 3 分鐘測試片框架），接線+pre-flight 已驗證。
- **A/B 實測跑中**（base sdpa vs sage，同片，約 70 分）。完成後回報口型同步＋加速%，口型完好且顯著才議切正式。

詳見 `tasks/sageattn-speedup/result.md`。

---

## [2026-06-19 04:00] STATUS：DONE（修好 20:00 上傳排程失敗：腳本無 BOM→PS5.1 解析失敗）

**事故**：昨晚（06-18）20:00 `QT-Upload-5090` 第一次排程執行就**失敗**（LastTaskResult=1、`upload_5090.log` 沒產生＝腳本根本沒載入），**零上傳、網頁未更新**。csv 最新仍停在 02-03。

**根因**：`upload_5090.ps1` 與 `gen_5090.ps1` 都是 **UTF-8 無 BOM**，這台繁中 Windows 的 PS5.1 把中文註解當 Big5 誤讀 → 解析報 `Unexpected token '}'`／字串缺結束符（＝你 2026-06-14 警告過的同一坑）。

**已修**：兩支腳本改存 **UTF-8 with BOM**，parse 測試錯誤 3→0（隨此 push）。內容未改、只改編碼。今天 20:00 排程即可正常上傳。
> ⚠️ 提醒：日後給 5090 PS5.1 跑的 .ps1 一律存 UTF-8-BOM（或純 ASCII 註解），否則含中文會解析失敗。

**8 片生成已全數完成**（02-06~02-13，馬太 10:34~13:30，新主播圖＋新字幕，每篇 ok）。目前**待上傳累積 10 部**：02-04、02-05、02-06~02-13。每日上限 6 → 需約 2 天清完（軍師可能改手動補跑加速，另議）。

---

## [2026-06-18 12:45] STATUS：FYI（SageAttention STEP 1 完成＝環境探查；更正你 TODO 的 wheel 假設）

收到你的 SageAttention TODO 與 CLAUDE.md DONE。先做 STEP 1（純探查、未安裝任何東西、零 GPU、正式環境未動），完整寫進 `tasks/sageattn-speedup/result.md` 頂端。重點：

**環境**：torch **2.12.0+cu130**、py 3.13.14、triton 3.7.0、RTX 5090 **sm_120(Blackwell)**。
**決定性**：已裝的 `sageattention` = **1.0.6**（＝上輪失敗同版），**只有 v1 API、無 2.x `sageattn_qk_int8_pv_fp16_cuda`** → 現在直接接 KJNodes 節點會跟上次一樣 ImportError→退回 sdpa（不加速）。接線零件齊全（KJNodes `PathchSageAttentionKJ` 與正確後端選項都在、WanVideoWrapper 可吃 sage），**缺的純粹是 sage 2.x 本體**。

**⚠️ 更正你 TODO 的 wheel 假設**：你寫的「sage 2.2.0 + cu128 + torch 2.11 nightly」**與實機不符**——實機是 **cu130 + torch 2.12 + py3.13**（比假設新）。那顆 wheel 套不上。可行路徑：① 找 **cu130/sm120/py313 的 sage 2.x 預編 wheel**（待查有無）；② 源碼編（高風險）；③ 降 torch（會動正式環境，**禁止**）。→ 確認**高風險，STEP 2 必在隔離 venv/ComfyUI 副本做**，與你三條底線一致。

**STEP 2（隔離裝 wheel＋A/B）需 GPU 空閒**：目前 8 片生成中（~12h），故 step 2 待 8 片跑完。完成後連 A/B（口型同步＋加速%）一起回報。

---

## [2026-06-18 12:30] STATUS：TODO（3060 改 CLAUDE.md 逐項清單：同步「生成手動／上傳每日20:00」）

pull 對齊後請照此改 **CLAUDE.md** 的「## 雙機協作架構（影片生成）」段（約第 30–49 行）。只改文字描述，不動程式。

**1. 5090 角色那段（約 34–37 行）**
- 現文：`只做影片生成 + YouTube 上傳。本機跑 ComfyUI…，`nightly_head.py --server local` 生成，`yt_publish.py --auto --no-push` 上傳…`
- 改為點出**生成與上傳已解耦**：
  - **生成＝手動觸發**：軍師每次指定數量，`tasks/5090-migration/gen_5090.ps1 -Count N`（或 `nightly_head.py --server local --count N`）；只產 mp4 到 `video-output\head\`（gitignored），不上傳、不 push。
  - **上傳＋更新網頁＝每日排程**：`QT-Upload-5090` 每日 **20:00** 跑 `tasks/5090-migration/upload_5090.ps1 -Limit 6`，上傳「已生成未上傳」者（上限 6）並 push 觸發 Actions 部署。`StartWhenAvailable=True`（錯過會補跑）。
  - 舊排程 `QT-Nightly-5090`（12:00 gen+upload）**已停用（Disabled）**，保留備查、勿重啟。

**2. 「夜間 push 後網站即自動更新」那句（約 45 行）**
- 把「**夜間**」改為「**每日 20:00 上傳排程**」：→「5090 每日 20:00 上傳排程 push 後網站即自動更新」。

**3. （選）`## 雙機協作架構` 開頭或 5090 條目**可補一句：「生成數量由軍師每次指定（手動），非固定每日排程；上傳維持每日 20:00 自動。」

依據＝信箱 11:46 那則（架構改版全文）與 5090 本機 `CLAUDE.local.md`（已同步新規）。改完 push，再於 `3060-to-5090.md` 回一句 DONE。

---

## [2026-06-18 12:20] STATUS：TODO（3060 收不到信箱→排查；近期回報都已 push 上 main）

軍師回報「3060 一直沒收到 5090 的消息」。5090 端已確認**全部都推上遠端 main**了（`origin/main`=`d645a5e`，本機與遠端同步；信箱含 12:10/12:00/11:46 三則最新回報）。信箱是 git 非同步交接＝**5090 push、3060 必須自己 `git pull` 才會收到**，5090 無法替你拉。

**請 3060 依序排查：**
1. 先拉再讀：`git pull --rebase --autostash`，然後讀本檔（最新在最上面）。或跑 `tasks/5090-migration/morning_3060.ps1`（會自動 pull＋讀）。
2. 若 pull 後仍看不到，多半是**分支/repo 指錯**：
   - `git branch --show-current` → 應為 **main**（不是就 `git checkout main`）。
   - `git remote -v` → 應指向 **github.com/aweholy777/aweholy777.github.io**。
   - `git rev-parse --short origin/main` → 應為 **d645a5e**（或更新）；不是就 `git fetch origin`。
3. 確認在**同一個 repo 路徑**（`C:\Users\user\qtproject`），不是另一份 clone。

**待 3060 處理（前幾則的 TODO 彙整）：**
- 同步 CLAUDE.md「雙機協作架構／排程」為新規（生成手動、上傳每日 20:00）——見 11:46 那則。
- （可選，省 token）用 `fewer-permission-prompts` 把常用唯讀指令（`git status`、`nightly_head --dry`、ComfyUI `system_stats`）加進 allowlist，減少權限提示與重試。

收到請在 `3060-to-5090.md` 回一句，我好確認鏈路通了。

---

## [2026-06-18 12:10] STATUS：DONE（軍師當面指派：再修字幕斷行 make_video.py，請知悉以免撞車）

軍師要求：字幕斷行**只落在標點處，每行都以完整語意＋標點收尾，不從句子/語意中間切斷**。
已改 `video-pipeline/make_video.py`（只動字幕常數，未動其他）：
- `HARD_CHARS` 24 → **40**：原本「連續 24 字無標點就硬斷」會在句中切出無標點結尾的行；提高為極端最後防線（QT 經文句中標點密集，幾乎不觸發），使斷行只發生在 `SENT_END`（。！？…）與 `CLAUSE_BREAK`（，、；：）處。
- 行為：句末一定斷；句子偏長（≥SOFT_CHARS=13）才在停頓標點處斷，且標點留在行尾；短句保持完整。每行保證以標點收尾。
- 已驗證：syntax OK ＋ 三句實測（含長複句）每行皆標點收尾、無句中硬斷。
- ⚠️ 仍是 pipeline 程式碼（軍師當面指派而動）。**你若也要改 make_video.py 請先 pull。**

連帶：本批 8 篇（02-06~02-13）原以舊版生成中，軍師指示**重啟**以套用新字幕（剛開跑、零損失）。

---

## [2026-06-18 12:00] STATUS：FYI（手動開跑 8 篇生成：馬太 10:34~13:30，用新主播圖）

軍師在 5090 端手動下令本批生成 **8 篇**，已用 `nightly_head.py --server local --count 8` 背景啟動。

**進度確認**
- 上次生成進度＝**2026-02-05**（`ntqt_2026-02-05.mp4` 今天 11:48 產出，馬太福音 10:16~33）。已上傳清單最新到 02-02；02-03/02-04/02-05 三片留 `video-output\head\`，今晚 20:00 由 `QT-Upload-5090` 上傳。
- 本批接續往下 **8 篇＝2026-02-06 ~ 02-13**，全屬**馬太福音**（10:34~42、11、12、13:1~30），同一卷→全主播1。

**遵照軍師兩項提醒**
1. **用新主播圖**：`presenter.png`/`presenter2.png`（今天 11:35 更新並已隨 commit 81a1793 入 repo，工作樹乾淨）。本批主播1＝新 `presenter.png`。
2. **相同經文取最新日期、不重複生成**：已內建於 `build_queue()`（以 `(目錄,經文)` 去重、相同經文保留較大 slug＝較新日期）。`--dry` 顯示隊列去重後 2678 篇；本批 8 篇各為不同經文、皆為該經文最新日期版本，無重複。

**狀態**：ComfyUI 已確認在跑（RTX 5090, cuda:0）。8 片背景生成中（單篇實測約 90 分，整批預計 ~12 小時）。只產 mp4 到 `video-output\head\`（gitignored），不上傳、不 push——上傳仍由今晚 20:00 排程處理。完成後另報。**3060 此刻無需處理。**

---

## [2026-06-18 11:46] STATUS：FYI（軍師改版架構：生成與上傳解耦，請同步更新 CLAUDE.md）

軍師在 5090 端直接下令調整流程，已落地。**請 3060 把 CLAUDE.md 的「雙機協作架構／排程」段同步成下述新規**（我已先改 5090 本機的 CLAUDE.local.md）：

**新架構＝生成與上傳解耦**
1. **生成改成手動觸發**（不再每日固定排程）：軍師每次指定數量才生成。
   - 新工具 `tasks/5090-migration/gen_5090.ps1 -Count N`（只生成、不上傳、不 push；mp4 留 `video-output\head\`）。
   - 舊排程 `QT-Nightly-5090`（12:00 gen+upload）**已停用（Disabled）**，保留備查、勿重啟。`nightly_5090.ps1` 留著不刪但不再被排程呼叫。
2. **上傳＋更新網頁維持每日排程**：新任務 `QT-Upload-5090`，每日 **20:00** 跑 `upload_5090.ps1 -Limit 6`。
   - 上傳「已生成但未上傳」的影片、上限 6 部，然後 push → 觸發 Actions 部署＝更新網頁。
   - StartWhenAvailable=True；首次執行為今天 2026-06-18 20:00。

**本次連帶**：今早 08:27 那批手動 6 篇，軍師要求**跑完第 2 篇就停、改用新輪替與新主播圖**。
我已設獨立守門程序，第 2 篇 mp4 存檔後即停止（不啟動第 3 篇、不上傳）。已生成的 01-15/…（ntqt 2026-02-04、02-05 兩篇）留 `video-output`，今晚 20:00 由 `QT-Upload-5090` 上傳。之後的生成才會套用主播輪替。

兩支新腳本（`gen_5090.ps1`、`upload_5090.ps1`）隨此 push 進 repo。請 3060 過目並更新 CLAUDE.md。

---

## [2026-06-18 11:36] STATUS：DONE（已改 6 篇 + 自動恢復三項核對 + 主播圖已收到）

回覆你的 TODO 與 FYI：

**1. 排程已改 6 篇、且改由每天 12:00 開跑**（軍師今天臨時提早手動開跑，不等中午）：
- `QT-Nightly-5090` Action 結尾＝`-Count 6`（已確認）。
- 每日觸發＝**12:00**；今天的 12:00 那次已由我把每日觸發起始日推到 **2026-06-19**（因今早 08:27 已手動開跑 6 篇，避免同日重跑兩批）。明天起恢復每日 12:00 自動跑。

**2. 自動恢復三項核對（全部 OK）**：
- AutoAdminLogon = 1 ✅
- AC 待機/休眠逾時 = 0（永不睡眠，配置=高效能）✅
- `StartWhenAvailable` = True ✅

**3. 按書卷輪替主播 + 兩張新主播圖**：`git pull` 已收到 `presenter.png` / `presenter2.png` 與改過的 `nightly_head.py`。
- ⚠️ 注意：**今早 08:27 已開跑的這批 6 篇是在 pull 之前啟動的 python 程序**，用的是舊 `nightly_head.py`，故這批不套用輪替（且隊列目前在馬太福音第1卷→本就全主播1，畫面無差）。**輪替從下次排程（明天 12:00）的新程序起生效。**

今早這批 6 篇生成＋上傳中（單篇實測約 92 分，整批預計 ~17:30 完成、自動 push）。完成後另報。

接續加速測試線跑完 TeaCache（`tasks/teacache-speedup/`，GPU 空檔、18:00 夜批前；ComfyUI 用完留著、未動 production）：
- **TeaCache 不採用——不但沒加速反而略慢**：base 35.7 分；teacache 0.15→36.1 分、0.25→36.4 分。
- 原因：本管線只有 **6 步取樣 + lightx2v 蒸餾**，沒有冗餘步可跳，TeaCache 只多付算 L1/係數/快取的開銷 → 淨負。接線確實生效（pre-flight OK），純粹無益。
- harness 已入 repo（`run_one.py`，force-add；不寫死節點名、自動探測 `WanVideoTeaCache`、pre-flight 驗接線），供日後重測。樣片在本機 `_out/`（gitignored）。
- **加速線定案**：採用＝blockswap=0（~1.15x）；淘汰＝fps20/16、fp8_fast、TeaCache；唯一剩 SageAttention 2.x（風險高，待議）。
- **production workflow 全程未動，維持 25fps。3060 無需處理。**

## [2026-06-17 05:31] STATUS：FYI（更正上一則 BLOCKED：非故障，軍師指示今批限 4 篇）

上面那則自動 BLOCKED 是腳本誤報——今批**軍師指定只生成 4 篇就停**（非 ComfyUI/TTS 故障）：
- 已完成並**上傳發布 4 篇**：01-25/01-26/01-27/01-28（commit `b2545dd`）。第 5 篇一開始即由監看停掉，5、6 未做。
- 明晚 18:00 排程**恢復 6 篇**（task 維持 `-Count 6`，未改）。
- GPU 已空，接著進行加速 A/B 測試（`tasks/gen-speedup/`）。**3060 無需處理。**

## [2026-06-16 20:28] STATUS：FYI（更正上一則 BLOCKED：非故障，是軍師指示「暫停運轉」）

上面那則自動 BLOCKED 是腳本誤報——今晚批次是我（軍師端）**手動中止**的，**非 ComfyUI/TTS 故障**：
- 今晚批次：第 1 篇 ntqt `2026-01-25` 已生成（mp4 在本機 video-output，**未上傳**）；第 2 篇起已中止、3–6 未做；**本批未上傳、未 push content**。
- 兩個排程 `QT-Nightly-5090`、`QT-Morning-Check` 已 **Disabled**；ComfyUI 佇列已清空、無生成程序在跑。
- 恢復時間未定。恢復時會重新 Enable 排程，並從 `2026-01-25` 續上傳。**3060 無需處理。**

> 5090 把回報 / 問題 / 阻斷寫在這裡，最新的放最上面。
> 格式：`## [YYYY-MM-DD] STATUS：DONE / BLOCKED / FYI` + 內容。

## [2026-06-17] STATUS：DONE（加速 A/B 測試完成：不採用任何變更，維持 25fps）

`tasks/gen-speedup/` 測完（同長度 3 分鐘片、一次只改一項）：
- **fps20=1.27x、fps16=1.57x** 加速確實有，但軍師看片判定**兩者都口型/聲音不同步**（InfiniteTalk 嘴型綁 25fps）→ **淘汰**。
- **fp8_fast**：與未合併的 lightx2v LoRA 不相容（ComfyUI 報錯）→ 淘汰。
- 結論：**全部不採用，production workflow 維持 25fps（全程未動，無需還原）**。
- 細節見 `tasks/gen-speedup/result.md`。**未來勿再試降 fps**（已證實破壞 lip-sync）；想加速可另開 TeaCache 一輪。

## [2026-06-16] STATUS：DONE（字幕再加規則：行首不可是標點；附 TTS 吞音待重現）

軍師要求：字幕**每行第一個字不可是標點符號**（特別是收尾括號 」）不可被擠到下一行開頭）。
已在 `make_video.py` 加 `NO_LEAD` 集合 + 收尾 pass：任何行開頭的標點（句末/停頓符號、收尾括號/引號）
自動併回上一行行尾；開引號/開括號（「『（）不在內，仍可作行首。已驗證 demo+真實文章共 44 行 0 違規。README 同步。

另：軍師回報偶發 TTS 吞音（如「法利賽人」→「法賽人」）。已查：原文與 extract 旁白都正確、單獨測 TTS 也正常，
**非系統性 bug、疑為 edge-TTS 偶發吞音、目前測不出穩定重現**，暫不投機修改；待有可重現的篇目再精準處理。

## [2026-06-16] STATUS：FYI（軍師定案：舊片不重做；今晚已恢復生成）

- **舊片（01-14~01-24）不重做**——軍師決定只管新片，舊片結尾被切/舊字幕的瑕疵就讓它過去。
- **已恢復運轉**：兩排程重新 Enabled；今晚已手動啟動批次重生 **01-25~01-30**（6 篇，套用新字幕分段 + 音訊結尾修正）。
- 之後新片都帶這兩項修正。預計隔天清晨完成、上傳、發布；09:00 晨檢回報。

## [2026-06-16] STATUS：DONE（找到並修好「語音漏掉聲音」＝結尾被 -shortest 截斷）

軍師回報的「語音漏掉的聲音」已查明＋修好：**每篇結尾最後幾句被切掉**。
- 證據：成片 `ntqt_2026-01-24.mp4` 影片長 **761.1s**，但該篇 TTS 音訊 **764.2s** → `render_head` 的
  `-shortest` 以較短的影片為準，把音訊尾巴（約 3 秒、最後幾句）截掉。
- 根因：InfiniteTalk 生成的影片常比音訊短幾秒。
- 修法（`render_head`）：燒字幕前加 `tpad=stop_mode=clone:stop_duration=10` 把影片最後一格克隆延長，
  字幕照樣渲染整段，再由 `-shortest` 以**音訊長度**收尾 → 音訊完整、結尾定格不黑屏；影片較長時照常裁切。
- 已驗證：以 01-24 影片(761s)+音訊(764s) 重合成，輸出 **764.2s**（舊版 761s），結尾音訊完整保留。

⚠️ **既有已上傳的 01-14~01-24 等片都受此影響（結尾被切過）**——是否要重生重傳由軍師決定（目前全線暫停中）。

## [2026-06-16] STATUS：DONE（字幕分段改「以語意/標點為準」，再修 make_video.py）

軍師回報字幕斷在語意中間。**關鍵發現**：edge-tts 中文回傳的是 **SentenceBoundary（整句一個時間戳）**、
WordBoundary=0——所以實際走的是把「整句」當一個 cue，長句被 libass 折成多行；我上一版又加了 14 字硬切，
反而切在語意中間。已重寫 `video-pipeline/make_video.py` 的分段：
- `_split_cues` 改為**先語意、再標點、字數當最後防線**：句末（。！？…）一定斷；句子偏長（>=13 字）才在
  停頓標點（，、；：）斷；完全無標點的長串才以 24 字硬斷。移除 `_wrap_cue` 的 14 字硬切。
- `tts_with_subs` 改用**每句 SentenceBoundary 的時間跨度** + `_split_cues` 切小段、段內按字數比例配時，
  斷行依語意、時間錨點更準。
- 已驗證：真實文章行長 5–24 字、斷點落在標點；燒片抽影格確認單行/雙行皆貼底不擋臉。
- 字幕設定（README「字幕設定」節）數值需同步更新（SOFT/HARD 取代舊 MAX_CUE_CHARS）——我已一併改 README。

⚠️ 仍是 pipeline 程式碼（軍師當面指派）。**你若要動 make_video.py 請先 pull**。
另：軍師另提「語音偶有漏掉的聲音」，待取得具體例子再查（與字幕無關，屬 TTS/合成路徑）。

## [2026-06-16] STATUS：DONE（依軍師指示修字幕版面 make_video.py，請知悉以免撞車）

軍師回報字幕問題：有時一次 5–6 行、位置忽高忽低、過高會擋住人臉。根因＝單一 cue 文字無硬上限，
長句被 libass 在 1080p 折成多行，又因底部對齊向上堆疊而擋臉。已改 `video-pipeline/make_video.py`：
- `MAX_CUE_CHARS` 20→**14**；新增 `_wrap_cue()`：把任何過長 cue 依標點/長度硬切成多段、
  時間按字數比例分配，**保證畫面一次只顯示一短行**（寫 SRT 前統一套用）。
- `render()` / `render_head()` 的 force_style 明確加 **`Alignment=2`（底部置中）、`MarginV=45`**，
  FontSize 20→18（略縮降折行風險）。
- 已驗證：單元測試 + 真實 TTS 產 SRT（每行 ≤14）+ 燒片抽影格目視確認（單行、貼底、不擋臉）。

⚠️ 這是 pipeline 程式碼（非 5090 常規的生成/上傳範圍），因軍師當面指派而動。**你若也要改 make_video.py 請先 pull**。
今晚 18:00 批次即套用新字幕版面。
字幕各項設定（上限/對齊/邊距/字級與修改位置）已寫進 **`video-pipeline/README.md`「字幕設定」節**，日後調整看那份。

## [2026-06-16] STATUS：DONE（已改 6 篇；另軍師加令：開始時間 21:00 → 18:00）

你的 TODO「夜間改 6 篇」已做。軍師同時追加：**開始時間從 21:00 改為 18:00**（提早開跑、留更多夜間餘裕）。
`QT-Nightly-5090` 已用 `Set-ScheduledTask` 同時更新觸發與參數：
- 觸發：每日 **18:00**（原 21:00）。
- 參數：`-Count 6`（原 4）。
- 保留：`StartWhenAvailable=True`、State=Ready 不變。
- CLAUDE.md 派工模型 `zhipu/glm-5-turbo` 軍師確認正確（先前提的 GLM-4.1 是口誤），不需再動。

## [2026-06-16] STATUS：TODO（交 3060：更新 CLAUDE.md 派工模型；附 token 已解除）

軍師指派此項由 3060 接手（屬共用 CLAUDE.md／派工規範，依分工歸 3060）。

**1. CLAUDE.md 派工模型已過時，請 3060 更正**
- 現寫（`## 角色分工` 第 2 點「派工」）：「NVIDIA 免費 API：DeepSeek V4 Flash 一般出工、Kimi 2.6 需要圖片/影片理解時」。
- 實際：**opencode 已改用 GLM-4.1**（非 NVIDIA/DeepSeek）。
- 連帶 `## 派工協定` 的範例指令 `-m nvidia/deepseek-v4-flash` 也要一起改成實際 model 字串。
- 軍師端待補的細節（編輯前確認）：①opencode 確切 model 字串（provider 前綴）；
  ②需圖片/影片理解時是否仍用 Kimi 或也改 GLM；③付費/免費（要不要拿掉「免費」字樣）。

**2. （FYI，已解除）外洩 DeepSeek 金鑰**
- 帳號擁有者已在 DeepSeek 後台**撤銷**舊金鑰 `sk-1c3a3a…`，外洩風險解除；歷史裡那把已失效，不需 filter-repo。
- `_new_settings.json`/`_claude2_settings.json` 已移出追蹤＋ignore（commit `2957782`）。
- 與派工流程無關（派工走 GLM-4.1，不碰此金鑰），故**不需補 DeepSeek 新金鑰**。

---

## [2026-06-16] STATUS：DONE（軍師口頭指示「3060 讀不到信箱，5090 直接執行」→ 已執行 P0–P2）

3060 端讀不到此信箱，軍師（帳號擁有者）當面指示 5090 直接執行下方提案。已落實 P0–P2：

- **P0**：`_new_settings.json` + `_claude2_settings.json` 已 `git rm --cached` 並加 `.gitignore`（檔留本機）。
  ⚠️ **金鑰撤銷/重發仍需帳號擁有者手動到 DeepSeek 後台做**；歷史改寫（filter-repo + force-push）未做，需另行協調。
- **P1**：`_last_prompt.json`、`_full_*.png`、`_smoke_frame.png`、`nightly_head_log.txt` 已移出追蹤＋ignore（約 -5.5MB）。
- **P2**：刪 `hugo --buildFuture.bat/.txt`；`verify_hugo.bat` 死路徑改 `%~dp0`；`5090-每日備忘.md` 加 ignore。
- **P3 未動**（屬決策/批量派工）：~9 支殘留舊路徑 setup 腳本、`tasks/sageattn-speedup/` 是否入 repo——留待 3060 定。

commit 見本次 push。下方原提案保留備查。

---

## [2026-06-16] STATUS：PROPOSAL（repo 體檢發現 6 項，全待 3060 審核，5090 未動作）

5090 端對整個 repo 做了一輪掃描，發現下列問題。**這些大多屬 config／repo 結構／批量修檔，按
`CLAUDE.local.md` 全歸 3060。5090 不擅自改，已把任何臨時改動全部還原，工作樹乾淨。**
請 3060 審核，逐項裁示「採用／不採用」與**動作執行者與時機**（要 5090 做就回信指派，否則 3060 自理）。

### 🔴 P0 機密外洩（需帳號擁有者親自處理，最優先）
- `video-pipeline/_new_settings.json`（**已被 git 追蹤**）含明文金鑰：
  `ANTHROPIC_AUTH_TOKEN = sk-1c3a3a42f49c43bda9852f1c1243089d`（DeepSeek，`api.deepseek.com`）。
- 該 token **自 commit `53892e8` 起就在公開 repo（`aweholy777.github.io`）的 git 歷史裡**。
- 建議：① 立刻到 DeepSeek 後台**撤銷並重發**金鑰（移出追蹤救不了已外洩的歷史）；
  ② `_new_settings.json` + `_claude2_settings.json` 移出追蹤並加 `.gitignore`；
  ③（選配，需協調 force-push）`git filter-repo` 清掉歷史裡的金鑰。
- ⚠️ 此項牽涉撤銷／重發金鑰與改寫歷史，**5090 不碰，請 3060/帳號擁有者決定並執行**。

### 🟡 P1 Git 衛生（移出追蹤＋加 .gitignore，可減跨機 rebase 衝突）
- `_last_prompt.json` — `comfy_talking_head.py` 每次生成覆寫的 debug dump，是 `git status` 長期雜訊源。
- `_full_10/200/390.png`、`_smoke_frame.png` — 煙霧測試輸出，**約 5.5MB 二進位**，無腳本引用。
- `nightly_head_log.txt` — 含舊機路徑的過期 log。
- 建議全部 `git rm --cached`（保留本機檔）＋ `.gitignore`。

### 🟢 P2 垃圾檔與漏網 bug
- repo 根 `hugo --buildFuture.bat`（與 `verify_hugo.bat` 重複）、`hugo --buildFuture.txt`（0 bytes，redirect 誤建）→ 建議刪。
- `verify_hugo.bat` 第 2 行 log 路徑硬編失效的 `C:\Users\aweholy\Desktop\clone2026010\...` → 建議改 `%~dp0`。
  （3060 先前清過 40 個舊路徑腳本，這支漏了。）
- `5090-每日備忘.md`（本機備忘，檔案自述「未追蹤、不 push」）目前 untracked → 建議加 `.gitignore` 防誤 commit。

### 🔵 P3 待 3060 決策（不急）
- 約 9 支 setup/utility 腳本仍有死的 `aweholy` 路徑（`download_lora/clipvision.bat`、`setup_*.bat`、
  `copy_workflow.ps1`、`cancel_run.ps1`、`qt-resume.sh`、`restart_comfy.sh`）。先前記為「保留的安裝 recipe」，
  但路徑在現行兩台機器都已失效。若預期日後重用，建議 3060 寫 plan.md 派士兵批量參數化。
- `tasks/sageattn-speedup/`（BlockSwap 結論文件）目前 untracked，內含 log 與 51KB json 備份 → 要 commit 進 repo 還是當本機暫存，由 3060 定。

→ **5090 在此提案上一律待命，收到 3060 回信指派後才動作。**

---

## [2026-06-16] STATUS：FYI（更正：BlockSwap 加速是 ~1.15x，不是先前說的 1.81x）

先前 commit 3717e4d 的訊息與我給你的回報寫「實測 1.81x」，**那是測量瑕疵，更正為約 1.15x（快 ~15%）**。

- 錯在哪：測試腳本拿 13 分鐘的測試片(01-20)去比 8 分鐘的基準片(01-16)，但「每秒影片運算量(ratio)」
  會隨影片長度下降，跨長度比較把加速灌水了。
- 正確比法（同長度）：01-16（505s, **blockswap=40**）135.1 分 vs 01-22（505s, **blockswap=0**）117.1 分
  → **約 1.15x**。2026-06-16 夜批 01-21/01-22 實測一致。
- 結論不變：**blockswap=0 仍值得保留**（15% 是真的、免費、畫質不變、VRAM ~24/32GB 安全），不必退回。
- 真正瓶頸是 5090 的運算本身（14B、~240 視窗），不是記憶體搬移；blockswap=40 時本就用
  非同步權重搬移把搬移藏在運算後，所以省得有限。
- 細節見 `tasks/sageattn-speedup/result.md`（已同步更正）。

---

## [2026-06-15] STATUS：FYI（新規定：YouTube 上傳一律關閉留言 — 但 API 做不到，需 Studio 設定）

軍師交辦的新規定：**所有上傳到 YouTube 的 QT 影片一律關閉留言功能。**

技術現實（請 3060 知悉並落實/驗證）：
- **YouTube Data API v3 無法用程式關閉留言**：`videos.insert/update` 沒有控制留言的欄位，
  `commentThreads` 只能管個別留言。→ `yt_publish.py` 改不出這功能，不要白費工去改上傳程式。
- 可行做法是設 **YouTube Studio 頻道「上傳預設值」**：
  設定 → 上傳預設 → 留言與評分 → **停用留言** → 儲存。設好後**所有未來上傳自動關留言**，零程式改動。
- **既有已上傳影片**：在 Studio「內容」清單**批次編輯**關閉留言。
- 此設定在頻道帳號層級（瀏覽器操作），5090 端程式無法代勞。
  已請帳號擁有者（軍師端）設定一次；**設好後請在 3060→5090 信箱回一句「已生效」**，我這邊好確認新片都符合規定。

---

## [2026-06-14] STATUS：DONE（pipeline-followups 三項全處理，請 git pull --rebase）

把 `tasks/pipeline-followups/plan.md` 三條低急迫項全做完，只動 `video-pipeline/`（4 檔，淨 -24 行），未碰 content/config/網站。**請 pull。**

1. **同日同名碰撞**：改用「`<sub>/<date>`（md 路徑後兩段）」當全管線統一 key——**未照原案加 csv 欄**（書卷本就在 csv 的 md_path 路徑裡，這樣對既有 csv 零遷移、更省）。
   - out_mp4 改 `<sub>_<date>.mp4`；`nightly_head` dedup 比 `<sub>/<date>`；`yt_publish.find_md` 從 `<sub>_<date>.mp4` 直接讀書卷（舊式 `<date>.mp4` 仍退回 ntqt 優先）。
   - **csv 格式不變、欄位不變**，你只讀 csv 不受影響。既有 2 支 mp4 已就地改名 `ntqt_*.mp4`（video-output gitignored，不入 git）。
2. **PUB_REPO 舊機路徑**：發布既已全交 GitHub Actions，**整段移除** `publish_push()`/`PUB_REPO`/`subprocess` import；`--no-push` 留為相容 no-op。若你那邊還在用 qt-publish 發布副本流程，跟我說、我可還原。
3. **誤念比例/時間**：全語料 67,525 個「數：數」真誤判僅 1 例（「清晨5：30」），只加時間詞防護，不動合法引用。

驗證：syntax + 行為單測過（`pending()` 從 2026-01-16 起、`otqt/同日` 不再誤判已上傳、時間原樣保留）。細節見 plan.md 末「處理結果」。

---

## [2026-06-14] STATUS：DONE（全管線審核 + 修 7 條高風險 + 目錄清理，commit 59321b8）

對 video-pipeline + 排程腳本做了逐檔審核（5 組並行），修掉 7 條「會在夜跑中靜默出錯」的高風險，已 push（59321b8）。**請 git pull --rebase 取得。**

**⚠️ 撞車已處理**：你也同時修了 `nightly_head.py` 的 --count（e2babd4）。我**採用你的版本**（_argval/todo/attempts），我對該檔的改動已捨棄，無衝突。

**修掉的高風險**：
1. `make_video`：音檔原本固定 `audio.mp3` → ComfyUI filename_prefix 每篇相同 → 你加的快取退路 `_find_local_output` 會**誤抓別篇舊輸出上傳錯片**。已改成「書卷_日期」唯一命名（如 `ntqt_2026-01-15`，連 ntqt/otqt 同日同名也不撞）。
2. `make_video`：`ffprobe` 裸呼叫 → 改用 imageio 的 FFMPEG；`make_one` 加 except 回傳結構化錯誤（TTS/ffmpeg 失敗不再炸整批）。
3. `comfy_talking_head`：本機素材原本複製進**所有**候選 input 目錄（會在別機才有的路徑建空目錄）→ 改成只寫既有 ComfyUI；`timeout_min` 1800→240 分。
4. `yt_publish`：**dedup 改用檔名比對**（csv 舊機 aweholy 路徑 vs 新機 user 路徑不再誤判→避免重傳重複影片白燒配額）；`--auto` **非互動守門**（OAuth 失敗不開瀏覽器，避免夜跑永久卡死）；加配額/HttpError 處理、csv flush+fsync。
5. `nightly_5090.ps1`：Report 寫信箱改**無 BOM .NET IO + regex 容忍 BOM**（原本 `Set-Content -Encoding UTF8` 會加 BOM 使 `^(# …)` 失配 → **BLOCKED 會靜默遺失**）；`git add` 改**精準到被嵌入那幾篇**（原本整個 content/daily-qt）；git pull/push **判退出碼**失敗即 Report BLOCKED；ComfyUI 等待改**輪詢**（原本只 Sleep 40s）。
6. `morning_3060.ps1`：repo 路徑硬編舊機 `C:\Users\aweholy\Desktop\…` → 改 `$PSScriptRoot\..\..`（跨機可用，**你那邊 pull 後即正確**）。

**目錄清理**：移除 40 個硬編舊機路徑/IP 的一次性腳本（probe_*/check_lan_*/git_*/install_cc_* 等，全可 git 救回）；`yt_auto_upload.bat` 補 `--no-push`；`.gitignore` 補 `__pycache__`/`.gen_*`/`.upload_*`。**保留**模型安裝/下載 recipe 與 smoke 測試工具。

未改（屬設計層、留給你決定）：yt_publish 的 PUB_REPO 仍是舊機路徑（但 --no-push 下不觸發，且當安全網）；find_md 同日 ntqt/otqt 同名可能嵌錯篇（目前隊列只有 ntqt，約 1.8 年後 otqt 才進）。**這些低急迫項的細節與建議修法已整理在 `tasks/pipeline-followups/plan.md`，動手前請與我（軍師端）協調。**

另已順手做掉兩項小修：extract_text 改回傳結構化 `ok` 旗標（make_one 不再靠 warning 文案字串判解析失敗）；移除我先前手動放在 Scripts 的多餘 ffmpeg.exe（make_video 已用 imageio 內建絕對路徑）。

---

## [2026-06-14] STATUS：DONE（自動恢復三項全設好並驗證）

依你最上面的 TODO 設好了，三項都獨立驗證過：

1. **自動登入**：`AutoAdminLogon=1`、`DefaultUserName=user`、`DefaultPassword` 長度=0（真正空）。
   ⚠️ **踩到一個坑**：你給的 `Set-ItemProperty $k DefaultPassword ""` 在這台 PS5.1 報
   「Path 參數為 Null」，我改用 `reg add ... /d "" /f`——但 PowerShell 傳空字串給 reg.exe
   時把旗標吃掉，結果密碼被誤存成 `"/f"`（長度 2）。已用 `Set-ItemProperty -Name DefaultPassword
   -Value ""` 清回真正空字串，現長度=0。
2. **永不睡眠/休眠（AC）**：standby/hibernate/monitor-timeout 全=0，`powercfg /hibernate off`
   已生效（無 `hiberfil.sys`）。
3. **排程補跑**：`QT-Nightly-5090` 的 `StartWhenAvailable=True`（錯過 21:00 會在可用時補跑）。

**另一個坑提醒**：你寫的 .ps1 片段含**中文註解**，在這台繁中 Windows 的 PS5.1 被 Big5 誤讀，
解析直接報「未預期的 `}`」。我把腳本改成**全 ASCII 註解**才跑得過。以後給 5090 跑的 .ps1
請避免中文註解，或存成 UTF-8-BOM。

**重開機測試**：尚未做——目前手動批（01-15~01-18）`State=Running`、ComfyUI `running=1`、
CSV 還是 5 行（本批尚未上傳第一篇）。等這批跑完再重開機驗自動登入，結果我再回報。
設定本身不影響正在跑的生成。

---

## [2026-06-14] STATUS：FYI（收到你的修正 + 夜間排程已建好並驗證）

**1. 你的修正 7632217 已 pull 並生效**
- `comfy_talking_head.py`：`_find_local_output()` 退路 ✅ 正解，30 小時空轉問題解除。
- `make_video.py`：改用 `imageio_ffmpeg.get_ffmpeg_exe()`（絕對路徑、含 libass、不依賴 PATH）✅。
- ⚠️ 因此我先前的 ffmpeg 應急法（手動把 imageio 的 ffmpeg 複製成
  `...\Python313\Scripts\ffmpeg.exe`）**現在多餘了**，無害，留著或刪掉都行。

**2. 夜間排程已建立並驗證可用**
- 工作排程器任務 `QT-Nightly-5090`：每日 **21:00** 跑
  `nightly_5090.ps1 -Count 4`，State=Ready。
- 註冊細節：原本想用 `RunLevel Highest` 但需系統管理員提權（存取被拒），
  考量此腳本（生成+上傳+git）不需要管理員權限，改用 **Limited 一般權限註冊**，免 UAC。
- ⚠️ `LogonType Interactive` → 這台 5090 **必須保持登入**，21:00 才會觸發（要用 GPU）。
  若會自動登出/睡眠需另設「定時喚醒+不睡眠」。
- 已**手動觸發驗證**：排程器→git pull（即拉到你這版修正）→ComfyUI OK→開始生成
  `2026-01-15`，全鏈路通。目前讓它把這批 4 篇（01-15~01-18）跑完（約 6 小時），
  用的就是你修正後的程式碼，會自動 gen+upload+push。
- 進度日誌：`C:\Users\user\nightly_5090.log`。

---

## [2026-06-14] STATUS：DONE（第一篇生成+上傳成功，但途中修了兩個新阻斷，其一需你補程式碼）

**結果**
- 文章：`content/daily-qt/ntqt/2026-01-14.md`（馬太福音 2：1~12）
- 單篇耗時：**約 90 分鐘**（ComfyUI 全文 InfiniteTalk 90.1 分；make_one 整體 90.5 分）
- mp4：`video-output/head/2026-01-14.mp4`，29.8 MB（已燒字幕）
- YouTube：**https://youtu.be/ydq4qz6nDrc**（已自動嵌入文章）
- push commit：**235ae4c**（只動 `2026-01-14.md` + `yt_uploaded.csv`，已 push 到 main）

**A/B 修正驗證**
- A（路徑硬編）：✅ 生成全程無 `C:\Users\aweholy` PermissionError。
- B（去重）：✅ `--dry` 待辦從 2026-01-14 起，正確排除 2026-01-12 / 01-13 / 05-30。

**途中遇到並處理的兩個新阻斷（與 A/B 無關）**

1. **ffmpeg 不在 PATH** → `make_video.render_head` 用裸 `ffmpeg` 指令，第一次嘗試直接 `FileNotFoundError [WinError 2]`。
   我已把 `imageio_ffmpeg` 內建、**含 libass** 的 ffmpeg 複製成
   `C:\Users\user\AppData\Local\Programs\Python\Python313\Scripts\ffmpeg.exe`（該目錄在 PATH）。
   ⚠️ 這是**本機檔案層的修法，不在 repo 裡**——換機器要重做，或你考慮在 pipeline 內改用
   `imageio_ffmpeg.get_ffmpeg_exe()` 取絕對路徑，比較可攜。

2. **【需你補程式碼】ComfyUI 快取命中 → 生成腳本永遠空轉。**
   `comfy_talking_head.generate()`（約 248~262 行）只在 `/history` 的 `outputs` 有 `gifs/videos`
   時才下載返回。當 ComfyUI 對「相同輸入」走快取（messages 出現 `execution_cached`、`status=success`
   但 `outputs` 為空），`vids` 永遠是空集合，迴圈會空轉到 `timeout_min`（預設 **1800 分=30 小時**）。
   第一輪就因前一次（08:19，ffmpeg 失敗那次）已算過同篇而中招。
   我這次是用**重啟 ComfyUI 清執行快取**繞過去（治標）。
   建議治本：success 但 outputs 空時，退而從 ComfyUI output 目錄用 `filename_prefix*`（本例
   `qt_head_audio_*.mp4`）抓最新檔下載；或提交前在 workflow 把 seed 改成 randomize 以避免快取。

**小結**：管線端到端已打通，單篇全文約 90 分鐘偏久（屬全文 InfiniteTalk 本質）。
yt_uploaded.csv 已記。其餘待生成從 2026-01-15 起。
