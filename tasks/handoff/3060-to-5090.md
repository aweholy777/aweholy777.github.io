# 3060 → 5090 信箱（只有 3060 寫；5090 只讀）

## [2026-07-04] DONE：3060 內容/建置維護三項完成（全綠、無需變更）

收到你 07-04 的 TODO，三項都做完，結果全綠：

- **TODO 1 索引排序**：跑 `sort-daily-qt-indexes.js` → `moved: 0`（otItems 1982、ntItems 1495、unparsed 0）＝各書卷 `_index.md` 本來就排序正確，**無變更**。
- **TODO 2 建置健康（重點）**：全站 `content/daily-qt` 掃描 **126 個 `{{< youtube >}}` shortcode，126 個格式全正確、0 異常、0 重複 ID** → 建置不會因嵌入影片而壞。佐證：你 07-01~07-04 每日 20:00 push 都成功觸發 Actions 部署，若有壞 shortcode 網站早停更了。
- **TODO 3 進度對帳**：已生成 292 / 已上傳 127（csv）＝ backlog 約 165 部（約 27 天上傳量），生成大幅超前，健康。

因三項皆無檔案變更，本則僅為回報。生成/上傳續照舊，我這條線維持待命。

另（FYI）：07-03 已完成一輪 repo 清理（刪聖經難字注音大圖 49MB＋該頁、移除誤入庫的 statusline/ccc-switch、清舊 smoke/setup 腳本；commit `666f943`），你 pull 後會看到那些檔消失，屬正常。

---

## [2026-06-19] DONE：SageAttention 採用我背書 + 2 項衛生裁示 + 上傳順序讚

收到 STEP 2/3 與全機體檢。漂亮——**SageAttention 1.75x、口型 OK、減頭部晃動（audio_scale 0.8）我都確認採用**，正式 workflow 改動（attention_mode sageattn）背書，無需回退。下面那則 SageAttention TODO 視為完成。

**兩項衛生裁示（已由 3060 處理，隨此 push）：**
1. **AGENTS.md → 不追蹤**：它是 CLAUDE.md 的靜態複本、會過期，且若入庫會與你本機 untracked 同名檔 pull 撞車。已加進 `.gitignore`（`AGENTS.md`）。**唯一規範以 CLAUDE.md 為準**；各機要給 opencode/Codex 讀就自留本機版即可，不進 repo。
2. **SageAttention 測試殘檔 → 已補 ignore**：`.gitignore` 加 `tasks/sageattn-speedup/_*`，防那 26MB 樣片/輸入/結果誤 commit。`sage-venv`(33M)+`sage-wheels`(6M) 你留作回退我同意，不急著清。

另：**`yt_publish --auto` 改成最舊日期先傳（升序）我確認採用**——這正是系列照進度發布要的，今晚 20:00 一次補滿 02-04~02-07 缺口＋發 02-14，完美。

全機體檢全綠、今晚排程沒風險，辛苦了。

---

## [2026-06-18] TODO：安全試驗 SageAttention 2.x 加速（可回退、不影響每日運轉）

軍師定案：**值得試，但要在隔離環境試、保留回退**。先前淘汰的 fps/fp8_fast/TeaCache 都不適用本管線；SageAttention 是讓「每次注意力運算變快」（不靠跳步），**是唯一能加速 6 步蒸餾管線的方法**，5090(Blackwell) 實測約快 30~40%（~90 分 → ~55~65 分/支）。最大風險＝為了套用可能要動 PyTorch 版本而弄壞現在能跑的 ComfyUI——所以**全程隔離、正式環境不動，驗證通過才切換**。

請建 `tasks/sageattn-speedup/`（你那輪 sageattn 紀錄若還在可併用），按下列步驟做，全程把過程寫 `result.md`：

1. **先驗版本（不安裝任何東西）**：記錄現在正式 ComfyUI 的 `python -c "import torch;print(torch.__version__, torch.version.cuda)"` 與 GPU/CUDA。判斷能否對上現成 wheel（SageAttention 2.2.0 + cu128 + PyTorch 2.11 nightly，Blackwell sm_120）。對得上＝低風險；要升 nightly 才行＝高風險，**務必走隔離環境**。
2. **隔離安裝**：複製一份 ComfyUI 環境或開獨立 venv 裝 SageAttention（用現成 prebuilt wheel，不要自己從源碼編）。**正式環境完全不動**。
   - 參考：mobcat40/sageattention-blackwell（prebuilt wheel）、ComfyUI Discussion #11583（Blackwell 原生 Windows）。
3. **正確接法（避開已知地雷）**：**不要**用 `--use-sage-attention` 旗標（Triton 後端會讓 Wan 輸出全黑）。改用 ComfyUI-KJNodes 的「Patch Sage Attention」節點，後端設 `sageattn_qk_int8_pv_fp16_cuda`。
4. **A/B 驗證**：用你 TeaCache 那輪的測試 harness，同一篇真實 QT 各生一支（有/無 SageAttention）。比對：①口型/聲音是否仍同步（這是硬門檻，不同步就淘汰）②實際加速幾 %。抽影格＋看片。
5. **判定**：口型完好 **且** 加速顯著才採用；採用＝把節點接進 production workflow，並在信箱回 DONE（附前後耗時、A/B 片段位置）。否則回報「淘汰＋原因」，正式環境維持原狀。
6. **任何步驟弄壞隔離環境都無所謂**，只要 production 那套還能每日 20:00 上傳即可。期間每日 6 片照常，不要因測試中斷正式運轉。

排程提醒：生成現在是手動，你做這個測試不會跟夜批撞（已無 12:00 夜批）；GPU 空檔做即可。完成回 `5090-to-3060.md`。

**【3060 補充 12:45 — 收到你 STEP 1，修正 wheel 假設＋給你低風險路線】**
你 STEP 1 探查（實機 torch **2.12 + cu130 + py3.13 + sm120**、現裝 sage 仍 1.0.6）正確，我那顆「cu128/torch2.11」wheel 確實套不上。我查過了：
- **有 cu130 + cp313 + Blackwell(sm120) 的 SageAttention 2.2 預編 wheel**（不必源碼編）：
  - `github.com/ziggyxp/sageattention-blackwell-pt211-cu130-cp313`
  - `github.com/mobcat40/sageattention-blackwell`（含 cu130 wheel 與 build story）
- **唯一對版落差**：這些 wheel 是對 **torch 2.11** 編的，你實機 2.12。2.11 wheel 裝到 2.12 **可能 import 失敗**（ABI，不一定，值得先試）。
- **低風險解法（破解「高風險」）**：STEP 2 既然在**隔離副本**做，就「讓環境去配 wheel」——在隔離副本裡裝 torch **2.11(cu130)** 對齊現成 wheel 即可。我先前寫「禁止降 torch」是指**正式環境**；**隔離副本裡降 torch 完全 OK**，這樣就不必冒險從源碼編。

→ STEP 2 建議順序：①隔離副本直接試裝 ziggyxp cu130/cp313 wheel，看 import 過不過（快、碰運氣）；②不過就在隔離副本裝 torch 2.11(cu130) 對齊 wheel（正式環境不動）；③兩條都不行才源碼編。8 片跑完再做、不急。

---

## [2026-06-18] DONE：CLAUDE.md 已同步「生成手動／上傳每日20:00」+ 你的拆開實作我確認採用

收到你 9b411f7 整批（字幕斷行、8 篇開跑、收信排查、CLAUDE.md 改法清單）。逐項回覆：
- **CLAUDE.md 已照你 12:30 的逐項清單改好**（隨此 push）：5090 條目改成「生成＝手動 `gen_5090.ps1 -Count N`／上傳＝每日 20:00 `QT-Upload-5090` `upload_5090.ps1 -Limit 6`／舊 12:00 `QT-Nightly-5090` 已停用」；第 3 點「發布自動」那句「夜間」改成「每日 20:00 上傳排程」；並補上「生成數量由軍師每次指定（手動）」。只改文字、未動程式。
- **你 e6a50a9 的拆開實作（gen_5090.ps1 / upload_5090.ps1 / 停用舊排程）我確認採用**——下面那則我先前寫的「拆開 TODO」你已自主做完，視為完成、無需再動。
- **字幕斷行 d645a5e（HARD 24→40、只在標點收尾）** 收到，採用。
- **8 篇開跑（馬太 02-06~02-13）** 知悉；02-03/02-04/02-05 三片今晚 20:00 由 upload 排程上傳，OK。

下面舊的「拆開 TODO」保留備查（已被你的 e6a50a9 實作取代）。

---

## [2026-06-18] TODO：生成與上傳「拆開」（軍師新定案，取代每日自動 gen+upload）

新架構——**生成手動、上傳自動**，請把現有 `nightly_5090.ps1` 拆成兩支（沿用你既有的 ComfyUI 輪詢／exit-code 判斷／Report-BLOCKED 等穩健邏輯）：

1. **生成＝手動觸發**：新增 `tasks/5090-migration/gen_5090.ps1`，參數 `-Count N`。
   - 流程：`git pull --rebase` → 確認 ComfyUI（沿用你的輪詢啟動）→ `nightly_head.py --server local --count N`。
   - **只生成，不上傳、不 push**；產出留 `video-output/head`（本機 gitignored）。
   - 使用者在 5090 輸入 `powershell -File ...\gen_5090.ps1 -Count N` 即生成 N 部（N 每次自訂）。

2. **上傳+發布＝每天 20:00 自動**：新增 `upload_5090.ps1` + 排程 `QT-Upload-5090`（每日 **20:00**）。
   - 流程：`git pull --rebase` → `yt_publish.py --auto --no-push --limit 6` → 只 `git add` yt_uploaded.csv + 被嵌入的那幾篇 → push（觸發 Actions 部署網站）。＝你現有 nightly 步驟 4–5＋錯誤處理。
   - **上傳順序依書卷/日期遞增（最舊先傳）**，讓上傳照進度走；若現在 `--auto` 是新的先（reverse），請改成舊的先。
   - limit 6（仍在 YouTube 每日配額內）。

3. **停用舊的合一排程**：`QT-Nightly-5090`（12:00 gen+upload）請 **Disable 或移除**——生成不再自動。

效果：你想生成時手動打指令累積存量；每天 20:00 自動從存量按進度上傳 6 部＋更新網站，生成與上傳節奏完全脫鉤。
完成後回信箱：列出 `gen_5090.ps1` 用法、`QT-Upload-5090` 的觸發/參數、舊排程已停用。

---

## [2026-06-18] FYI：按書卷輪替主播已實作 + 兩張新主播圖入庫

軍師補回「每卷書換一個主播」邏輯（先前幾輪改動中掉了，導致一直只用主播1）：
- `nightly_head.py`：`build_queue` 帶 `book_seq`（跨新舊約連續累計）；新增 `presenter_for()`——
  奇數卷→`presenter.png`(主播1)、偶數卷→`presenter2.png`(主播2)，presenter2 缺則退回主播1；
  生成時把選到的主播圖以 `bg=` 傳給 `make_one`（不再寫死 presenter.png）。
- assets：`presenter.png`(主播1，新圖) 與 `presenter2.png`(主播2，新圖) 兩張都已入 repo。
- 現況：隊列還在**第1卷（馬太福音）→全主播1**；**主播2 從第2卷（馬可福音）起**登場。
- 你下次 `git pull` 後即生效；`--dry` 會顯示每篇「第N卷 → 主播X」。

另：上面那則「每晚改 6 篇」TODO，你回到線上時一併套用（排程 `-Count 4→6`），完成各回一句即可。

---

## [2026-06-16] TODO：夜間排程改成每晚 6 篇

軍師決定把產能調到**每晚 6 篇**（跑完全站從 ~1.8 年縮到 ~1.2 年；6 篇/天仍在 YouTube 每日配額上限內）。
你目前 `QT-Nightly-5090` 排程帶 `-Count 4`，請改成 6。在 5090 本機 PowerShell 跑（不需提權）：

```powershell
$a = New-ScheduledTaskAction -Execute 'powershell.exe' -Argument '-ExecutionPolicy Bypass -File C:\Users\user\qtproject\tasks\5090-migration\nightly_5090.ps1 -Count 6'
Set-ScheduledTask -TaskName 'QT-Nightly-5090' -Action $a
(Get-ScheduledTask 'QT-Nightly-5090').Actions.Arguments   # 確認結尾是 -Count 6
```

改好在信箱回一句「已改 6 篇」。（3060 想從遠端改，但 SSH 過去時 5090 不在線，故交給你本機設。）

---

## [2026-06-16] DONE：CLAUDE.md 派工模型已更新為 GLM-5-Turbo（+ P0-P3 回覆）

你交辦的「CLAUDE.md 派工模型過時」已更正（隨此次 push）：
- 角色分工『派工』：DeepSeek/Kimi（NVIDIA 免費）→ **智譜 GLM-5-Turbo `zhipu/glm-5-turbo`（付費）**，一般出工與圖片/影片理解**皆用此模型**。
- 派工協定範例指令：`-m nvidia/deepseek-v4-flash` → `-m zhipu/glm-5-turbo`；保留「model ID 以 `opencode models` 為準」那句當保險。

**P0 金鑰**：帳號擁有者已撤銷舊 DeepSeek 金鑰（你回報的），歷史裡那把已失效、**不需 filter-repo**。你做的「移出追蹤 + ignore」採用。
**P1/P2**：你依口頭授權執行的 git 衛生與垃圾檔清理，我確認採用，無需回退。
**P3（低急迫，我先不派）**：~9 支死路徑 setup 腳本暫留（日後要重用再寫 plan 派工參數化）；`tasks/sageattn-speedup/` 你可自行 commit 進 repo 留檔（屬你的工作節點紀錄，我同意入 repo）。

---

## [2026-06-16] FYI：審核提案三項已過目確認，PUB_REPO 移除我背書

讀過 `tasks/pipeline-followups/plan.md` 的「處理結果」，三項都確認 OK、無需回退：
1. **同日同名碰撞**——用「書卷/日期」當 key、不加 csv 欄、零遷移，做法比原案更省，採用。
2. **PUB_REPO/publish_push 移除**——**我背書**：發布已全交 GitHub Actions（push main → `deploy.yml`
   自動部署），舊發布副本流程確定廢棄、3060 端也沒在用，移死碼無虞，不必還原。
3. **誤念比例/時間**——只加時間詞防護、不動 6.7 萬合法引用，合理，採用。

全部無待裁示。pipeline 比一開始更乾淨穩定，你那輪自主審核很到位。繼續每晚 4 篇即可。

---

## [2026-06-15] FYI：YouTube 關閉留言「已生效」

YouTube Studio 上傳預設值已設為**停用留言**，未來上傳自動關留言；既有影片也已批次關閉。
新規定已落實，你之後產的片都符合。也謝謝你查清楚 API 做不到、省了改上傳程式的白工。

順帶：你那個 BlockSwap 40→0 的 1.81x 加速很讚，已 pull 到 3060。繼續每晚 4 篇即可。

---

## [2026-06-14] FYI：nightly_head 修好了 —— 現在一晚產 N 篇（不再只 1 篇）

稽核發現 `nightly_head.py` 舊版**沒解析 `--count`**、生成迴圈第一篇就 `break`，所以每晚只產 1 篇
（你那次只出 `01-15` 就是這個原因，不是 4 篇）。已修正並 push（commit `e2babd4`）：
現在 `--count` / `--server` 真正生效、依 count 產出、單篇失敗自動跳過、連續失敗有上限。

`nightly_5090.ps1` 每次跑都會先 `git pull`，所以**下次排程（今晚 21:00）會自動拿到此修正、一晚產 4 篇**，
你不需手動做任何事。想立刻確認可跑 `python video-pipeline\nightly_head.py --dry`（只列隊列，不生成）。

---

## [2026-06-14] TODO：設「自動恢復」——讓重開機也能每晚穩定觸發（此帳號無密碼，可全自動）

帳號 `user` **無密碼** → 自動登入可直接設、沒有密碼外洩風險。下面三件事都需系統管理員權限，
請用一個「以系統管理員執行」的 PowerShell **一次做完**（會跳 UAC，按「是」即可，無密碼不需輸入）：

```powershell
# 1) 自動登入（開機自動進桌面；空密碼）
$k="HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon"
Set-ItemProperty $k AutoAdminLogon 1
Set-ItemProperty $k DefaultUserName "user"
Set-ItemProperty $k DefaultPassword ""
# 2) 永不睡眠/休眠（接電源）
powercfg /change standby-timeout-ac 0
powercfg /change hibernate-timeout-ac 0
powercfg /change monitor-timeout-ac 0
powercfg /hibernate off
# 3) 排程補跑錯過的（錯過 21:00 → 下次可用時補跑）
$t=Get-ScheduledTask -TaskName QT-Nightly-5090
$t.Settings.StartWhenAvailable=$true
Set-ScheduledTask -TaskName QT-Nightly-5090 -Settings $t.Settings
```

**驗證**：
- `(Get-ItemProperty $k).AutoAdminLogon` = 1
- `powercfg /query` 看 AC 待機逾時為 0
- `(Get-ScheduledTask QT-Nightly-5090).Settings.StartWhenAvailable` = True

若自動登入登錄機碼法無效（有些 Windows 版本擋空密碼自動登入），改用備援：執行 `netplwiz` → 取消勾「使用者必須輸入使用者名稱和密碼」→ 套用（空密碼直接按確定）。

重開機測試：應自動進桌面 → ComfyUI 自啟 → 21:00 排程會跑。完成把三項狀態寫回 `5090-to-3060.md`。
（正在跑 01-15~01-18 那批的話：設定不影響生成；要重開機測自動登入就等那批跑完。）

---

## [2026-06-14] DONE + 續跑：第一篇收到，已修掉你回報的兩個阻斷

收到你第一篇回報（2026-01-14，https://youtu.be/ydq4qz6nDrc，約 90 分），漂亮。
你回報的兩個阻斷我已治本並 push（從 origin 重建、保留你的字幕改良）：
- **ffmpeg**：`make_video.py` 改用 `imageio_ffmpeg` 內建的絕對路徑（含 libass），不再依賴 PATH。
- **ComfyUI 快取空轉**：`comfy_talking_head.py` 在 success 但 outputs 空時，改從本機輸出資料夾
  取回最新 `qt_head_*`；取不到就**立刻明確報錯**，不再空等到 30 小時逾時。
請 `git pull --rebase` 取得後即可正常續跑（下一篇從 2026-01-15 起）。
你本機那份手動放的 ffmpeg.exe 留著無妨，程式現在優先用 imageio 內建，不衝突。

---

## [2026-06-14] TODO：套用 A/B 修正後重跑第一篇（新約）

我（3060）已修好生成阻斷的問題 A（input 路徑硬編）與 B（重複判定），push 到 main（commit `5a50deb`）。請依序：

1. `cd C:\Users\user\qtproject` 然後 `git pull --rebase`。
2. 驗證 B：`python video-pipeline\nightly_head.py --dry`
   —— 待辦應**從 2026-01-14 起**，且不含 2026-01-12、2026-01-13、2026-05-30。
3. 確認 ComfyUI 在跑：`Invoke-RestMethod http://127.0.0.1:8188/system_stats`
   （沒回應就 `Start-ScheduledTask -TaskName 'ComfyUI'`，等 30 秒再試）。
4. 生成 1 篇並計時：`python video-pipeline\nightly_head.py --server local --count 1`
   —— 應不再出現 `C:\Users\aweholy` 的 PermissionError。
5. 上傳（不發布）：`python video-pipeline\yt_publish.py --auto --no-push --limit 1`
6. 依 `CLAUDE.local.md` 把 `video-pipeline/yt_uploaded.csv` 與被嵌入那篇 QT push 回 main。
7. 把「單篇耗時 / YouTube 連結 / commit hash / 有無異常」寫進 `5090-to-3060.md`（最新放最上面）後 push。

完成後狀態請改成 DONE。若中途卡住，改成 BLOCKED 並寫清楚卡在哪、貼錯誤訊息。
