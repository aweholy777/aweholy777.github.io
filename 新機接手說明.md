# 城市宣培中心網站 — 新機接手說明（單機作業）

> 給「接手這台電腦的 AI 助理」看的。
> 撰寫時間：2026-09-24　來源機器：原本的 5090（`DESKTOP-BFSJ95H`）
> **本專案已改為單機作業：這台（新機）現在是唯一的工作機，請接手全部。**

---

## 0. 一句話總結

這個資料夾（`qtproject`）是 **cmtc.tw** 網站的完整專案（Hugo 靜態網站 + GitHub Pages）。
**影片已全部生成完畢**，目前只剩「每天上傳 YouTube」與網站內容維護。交給你接手。

---

## 1. 接手時的現況（快照 2026-09-24）

| 項目 | 狀態 |
|---|---|
| **影片生成** | ✅ **全部完成**（新約 849 ＋ 舊約 1862 ＝ **2711 支**），未生成 0 |
| 新約 QT（849 篇） | ✅ **生成＋上傳都完成** |
| 舊約 QT（1862 篇） | 已上傳 **294**、已生成待傳 **1568**、未生成 0 → **只剩上傳**（2026-09-24 07:00 實測） |
| 上傳速度 | 約 **23–24 支/天**（每小時滴傳 1 支，每日上限 24） |
| 預計傳完 | 舊約剩 1568 支 → 約 **2 個月（~2026 年 12 月初）** |
| 影片生成排程 | **已停用**（`QT-GenLoop-5090` Disabled）——不需再生成 |

> 最新數字請看 `tasks\progress\_summary.txt`（或執行下面的重生指令）。

---

## 2. 這個專案是什麼

- **網站**：https://cmtc.tw/ （城市宣培中心）
- **技術**：Hugo 靜態網站、theme `mainroad`、部署到 **GitHub Pages**
- **Repo**：`https://github.com/aweholy777/aweholy777.github.io`（分支 `main`）
- **發布方式**：**push 到 `main` 就會自動部署**（GitHub Actions，`.github/workflows/deploy.yml`）。**沒有其他部署步驟。**
- **核心內容**：每日 QT 靈修文章兩個系列
  - `content\daily-qt\ntqt\` 新約（849 篇）、`content\daily-qt\otqt\` 舊約（1862 篇）
  - 每篇搭配一支朗讀影片（已上傳到 YouTube）
- **最完整的技術文件在專案內**：`HANDOFF.md`（請務必讀，這份只是「新機接手版」）

---

## 3. 新機要先做好的設定（重要，依序）

1. **Python 3.x**：本機 **conda 已退役**，統一用 **uv** 管理（不用系統 Python、不裝全域套件）。
2. **用 uv 建立專案 venv 並裝上傳套件**（`video-pipeline\requirements.txt` 只列了 `edge-tts`，上傳用的這三個要另外裝）：
   ```powershell
   cd D:\qtproject
   uv venv .venv --python 3.12
   uv pip install --python .venv\Scripts\python.exe google-api-python-client google-auth google-auth-oauthlib edge-tts
   ```
   之後**所有 Python 指令都用 `D:\qtproject\.venv\Scripts\python.exe`**（排程腳本已指向它）。
3. **確認 YouTube 憑證存在**（**機密，絕不可進公開 repo / 外流**）：
   - `video-pipeline\yt_token.json`
   - `video-pipeline\client_secret.json`
4. **確認影片在**：`video-output\head\*.mp4`（這是上傳的來源）
5. **確認能 push 到 GitHub**（`git push` 需要你的 GitHub 認證；若是複製整個資料夾，`.git` 已帶過來，但要確認 push 有權限）
6. **建立 Windows 排程任務**（見 §5）
7. 確認有網路。

> **本機實際值（2026-09-24 接手，機名 `GJH731`）**：使用者 `aweholy`、專案路徑 `D:\qtproject`、
> Python＝`D:\qtproject\.venv\Scripts\python.exe`（uv 建立的 venv，非系統 Python、非 conda）。
> `tasks\5090-migration\upload_5090.ps1` 內寫死的舊機路徑**已改成本機值**（見 §7.2）。

---

## 4. 上傳是怎麼運作的

```
Windows 排程 QT-Upload-5090（每小時 :05）
   └─ tasks\5090-migration\upload_5090.ps1
        ├─ git pull --rebase --autostash
        ├─ 檢查暫停旗標 C:\Users\user\upload_pause.flag（有且未過期就跳過）
        ├─ 檢查每日上限：過去 24h 已傳 ≥ DailyCap(24) 就跳過
        ├─ python video-pipeline\yt_publish.py --auto --no-push --limit 1
        │     （從 video-output\head\*.mp4 依「聖經卷序」挑 1 支上傳 YouTube，
        │       寫入 video-pipeline\yt_uploaded.csv）
        ├─ python video-pipeline\build_qt_library.py（重生 data\qtvideos.json）
        ├─ git add（yt_uploaded.csv + data\qtvideos.json + 被嵌入變更）→ commit → pull → push
        │     （push 即觸發 GitHub Actions 更新網站）
        └─ 把已上傳的 mp4 從 head\ 搬到 head\old\（歸檔、騰空間）
```

- **`video-pipeline\yt_uploaded.csv` 是「已上傳」的唯一權威清單**（在 git 裡）。
- 每支約消耗 YouTube API 100 點配額；真正瓶頸是頻道風控，不是配額。
- 碰到 YouTube 配額/上限錯誤（exit 2）會寫 `upload_pause.flag` **暫停 24 小時**，過期自動恢復。
- **影片嚴格按聖經卷序**上傳（不跳隊）。

---

## 5. 在新機建立上傳排程（Windows）

以系統管理員或目前使用者身分執行 PowerShell：

```powershell
$a = New-ScheduledTaskAction -Execute 'powershell.exe' `
     -Argument '-ExecutionPolicy Bypass -File "D:\qtproject\tasks\5090-migration\upload_5090.ps1" -DailyCap 24'
# 觸發 1：每小時 :05（長期滴傳）；起始時間要算在「未來」，否則會立刻補跑
$hourly = New-ScheduledTaskTrigger -Once -At (Get-Date).Date.AddHours((Get-Date).Hour + 1).AddMinutes(5) `
     -RepetitionInterval (New-TimeSpan -Hours 1) -RepetitionDuration (New-TimeSpan -Days 3650)
# 觸發 2：每次「登入後 5 分鐘」——關機重開／登出再登入後立刻接續進度，不必等整點
$logon = New-ScheduledTaskTrigger -AtLogOn -User ($env:USERDOMAIN + '\' + $env:USERNAME)
$logon.CimInstanceProperties['Delay'].Value = 'PT5M'
$p = New-ScheduledTaskPrincipal -UserId ($env:USERDOMAIN + '\' + $env:USERNAME) -LogonType Interactive -RunLevel Limited
$s = New-ScheduledTaskSettingsSet -StartWhenAvailable -MultipleInstances IgnoreNew `
     -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries -DontStopOnIdleEnd `
     -ExecutionTimeLimit (New-TimeSpan -Hours 2)
Register-ScheduledTask -TaskName 'QT-Upload-5090' -Action $a -Trigger $hourly, $logon -Principal $p -Settings $s -Force
```

- `StartWhenAvailable`：**關機/沒開機時漏掉的那一班，開機後會補跑一次**。
- **`AtLogOn` 觸發（延遲 5 分鐘）**：每次登入後自動跑一次，確保**關機重開／登出再登入後立刻接續進度**。
- `MultipleInstances IgnoreNew`：不會同時跑兩份。
- 上限是「**滾動 24 小時 24 支**」：關機期間少傳的**不會加倍補回來**，只是完成日往後順延；
  只要電腦開著，長期就是 24 支/天。
- 排程身分是 `Interactive`：**必須已登入才會跑**（螢幕鎖定沒關係，登出／關機不跑）。
- 觸發時間若設在「過去」，`StartWhenAvailable` 會讓它**立刻補跑**；起始時間務必算在未來。

選配（原機也有，但非必要）：
- `QT-DailyReport-5090`（每天 09:00 產生進度日報）
- `QT-ChatLogSync`（同步 AI 對話紀錄）
- `ComfyUI`、生成排程 **都不需要**（生成已完成）

---

## 6. 切換的安全步驟（**非常重要**）

> ⚠️ **同一時間只能有一台跑上傳！** 兩台同時跑會**重複上傳同一支影片**（因為兩邊都從同一份 `yt_uploaded.csv` 的舊狀態去挑）。

建議順序：
1. **在舊機（5090）停用上傳排程**：`Disable-ScheduledTask -TaskName QT-Upload-5090`
2. **複製整個 `qtproject` 目錄到新機**（含 `video-output\` 影片、`.git`、憑證）
3. **在新機設定環境**（§3）＋**建立排程**（§5）
4. **先手動測試一次**（見 §8 的測試指令），確認能上傳成功
5. 測試 OK 後，**正式讓新機的排程接管**
6. 舊機**保持停用/關機**，不再上傳

---

## 7. 關鍵雷區（別踩）

1. **不要兩台同時上傳**（見 §6）。
2. **路徑寫死**：`upload_5090.ps1` 原本寫死舊機路徑（`C:\Users\user\qtproject`、`C:\Users\user\upload_pause.flag`、`C:\Users\user\upload_5090.log`）。**2026-09-24 已改為本機值**：`$repo = D:\qtproject`、`$py = D:\qtproject\.venv\Scripts\python.exe`、log／旗標放 `C:\Users\aweholy\`。其餘舊腳本（`gen_5090.ps1`／`nightly_5090.ps1`／`update_comfyui.ps1`／`daily_report.ps1`）仍留舊機路徑，因生成已完成、目前用不到；日後要用再一併改。
3. **YouTube 憑證是機密**：`yt_token.json`、`client_secret.json` **不可進公開 repo、不可外流**。
4. **`.ps1` 腳本不要放中文**（Windows PowerShell 5.1 會因編碼解析失敗）；要嘛全 ASCII，要嘛存成「UTF-8 含 BOM」。
5. **Hugo 用 repo 根目錄那份 `hugo.exe`（釘死 v0.144.2）**，不要用 `winget` 裝新版（新版對含 HTML 的內容檔會有更嚴的 security policy，會誤判建置失敗）。建置指令：`.\hugo.exe --buildFuture`。
6. **文章不再內嵌 YouTube 影片**（影片統一在「QT影音庫」頁 `/qt-video/`）。新增文章不要加 `{{< youtube >}}`。
7. **敏感資料**（見證、代禱事項）不要交給雲端 AI；要用本地模型。
8. **每日QT 索引頁**由 `content\daily-qt\sort-daily-qt-indexes.js` 自動產生（同經文只列最新日期）；不要手改 `_index.md`。

---

## 8. 常用指令（在本機 `D:\qtproject` 底下執行）

```powershell
# Python 一律用 venv（uv 建的）
$py = D:\qtproject\.venv\Scripts\python.exe

# 目前進度
& $py tasks\progress\gen_progress.py
type tasks\progress\_summary.txt

# 手動上傳 1 支（測試用；`--no-push` 已棄用成 no-op，發布一律由 GitHub Actions 處理）
& $py video-pipeline\yt_publish.py --auto --limit 1

# 重生影音庫資料（影音庫頁面用）
& $py video-pipeline\build_qt_library.py

# 本機建置檢查（0 錯誤才算過）
.\hugo.exe --buildFuture

# 看排程狀態
Get-ScheduledTask -TaskName 'QT-Upload-5090' | Select-Object TaskName,State

# 手動觸發一次上傳排程
Start-ScheduledTask -TaskName 'QT-Upload-5090'

# 看 log
Get-Content C:\Users\aweholy\upload_5090.log -Tail 30
```

---

## 9. 重要檔案與路徑速查

| 用途 | 路徑 |
|---|---|
| 專案根目錄 | `D:\qtproject\` |
| venv Python（uv） | `D:\qtproject\.venv\Scripts\python.exe` |
| 影片（待傳/歸檔） | `video-output\head\`（＋ `head\old\`） |
| 已上傳清單（權威） | `video-pipeline\yt_uploaded.csv` |
| YouTube 憑證 | `video-pipeline\yt_token.json`、`client_secret.json` |
| 上傳程式 | `video-pipeline\yt_publish.py` |
| 上傳排程腳本 | `tasks\5090-migration\upload_5090.ps1` |
| 上傳 log | `C:\Users\aweholy\upload_5090.log` |
| 進度表 | `tasks\progress\_summary.txt`、`新約進度表.md`、`舊約進度表.md` |
| 影音庫頁面資料 | `data\qtvideos.json` |
| **完整技術文件** | `HANDOFF.md`（**必讀**） |

---

## 10. 接手後的第一步（Checklist）

1. `git status`、`git log --oneline -10` 了解現況。
2. 讀 `HANDOFF.md`（最完整）＋ `tasks\progress\_summary.txt`（進度）。
3. 確認 Python 套件、憑證、影片都在（§3）。
4. 執行 `python tasks\progress\gen_progress.py` 確認能跑。
5. **手動測試上傳 1 支**（`yt_publish.py --auto --limit 1`），成功後才建立排程。
6. **確認舊機（5090）的上傳排程已停用**，避免重複。
7. 建立 `QT-Upload-5090` 排程（§5），開始每天自動滴傳。
8. 有疑問或發現本說明與實際不符 → **直接更新這份文件與 `HANDOFF.md`**，讓下一個接手的人看到最新狀態。

---

## 備註：舊機（5090）的事
- 舊機曾負責**影片生成（ComfyUI + InfiniteTalk）**，但**已全部生成完畢，不需要再生產**。
- 若日後**新增了 QT 文章**需要生成影片，才需要恢復生成環境（ComfyUI + 模型，見 `HANDOFF.md` §3）；否則新機**不需要** ComfyUI、GPU、`torch` 等。
- 本專案已**取消雙機協作**，請以「單機」方式運作：這台新機就是唯一工作機。

---

## 附錄：2026-09-24 新機（GJH731）接手完成紀錄

- 接手機：**`GJH731`**，使用者 `aweholy`，專案 **`D:\qtproject`**；舊機 5090 已於當日 06:25 停用上傳排程。
- **Python 改用 uv**（本機 **conda 已退役**）：`uv venv .venv --python 3.12`，再裝
  `google-api-python-client`、`google-auth`、`google-auth-oauthlib`、`edge-tts`；
  `.venv/` 已加入 `.gitignore`。**所有 Python 指令一律用 `D:\qtproject\.venv\Scripts\python.exe`**。
- `tasks\5090-migration\upload_5090.ps1` 內寫死的舊機路徑已改為本機值
  （`$repo = D:\qtproject`、`$py = D:\qtproject\.venv\Scripts\python.exe`，log／暫停旗標移到 `C:\Users\aweholy\`）。
- **手動測試上傳成功**：`otqt_2025-04-02.mp4` → <https://youtu.be/0P9kRvhG318>
  （公開，標題「2025 – 04 – 02 QT 民數記 8：14~26」）；`yt_uploaded.csv`、`data\qtvideos.json`、
  push、mp4 歸檔 `head\old\` 全部正常。
- 排程 `QT-Upload-5090` 已建立：**每小時 :05** ＋ **每次登入後 5 分鐘**（`AtLogOn`，`Delay=PT5M`）、
  `-DailyCap 24`、`StartWhenAvailable`、`MultipleInstances IgnoreNew`；**實跑驗證 `LastTaskResult = 0`**。
  關機／登出後再回來，會在登入 5 分鐘內自動接續上傳。
- 排程 settings 補強（2026-09-24）：`-AllowStartIfOnBatteries -DontStopIfGoingOnBatteries
  -DontStopOnIdleEnd -ExecutionTimeLimit PT2H`。**本機有電池**，Windows 出廠預設是「用電池不啟動／
  切到電池就停止／閒置就停止」——會讓上傳**靜默停擺或中途被砍**，務必關掉。
- 停止追蹤 7 個 `video-pipeline\__pycache__\*.pyc`（早於 ignore 規則被納入版控，
  會讓每次上傳產生 dirty／autostash 噪音）。
- 讀 log 小提醒：`C:\Users\aweholy\upload_5090.log` 是 **ANSI（cp950）** 編碼，
  用 `Get-Content <path> -Encoding Default` 才不會亂碼（用 bash `cat`／`grep` 會看到亂碼，別誤判沒跑）。
- ⚠️ **待辦（人工）**：舊機 5090 **保持停用／關機**，切勿重啟其上傳排程——兩台同時跑會**重複上傳同一支**。

